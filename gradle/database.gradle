ext {
    database = [:]
}

database.driver = 'org.postgresql.Driver'
database.url = System.getenv('SPRING_DATASOURCE_URL') ?:'jdbc:postgresql://172.0.1.100/version_manager_test'
database.user = System.getenv('SPRING_DATASOURCE_USERNAME') ?: 'version_manager_test'
database.password = System.getenv('SPRING_DATASOURCE_PASSWORD') ?: 'password'
