// Because spring boot is opinionated, and I don't want their opinions.
ext['hibernate.version'] = deps.versions.hibernate
ext['jackson.version'] = deps.versions.jackson
ext['jooq.version'] = deps.versions.jooq
