apply plugin: 'java'
apply plugin: 'groovy'
apply plugin: 'local-dev'
apply plugin: 'kotlin'

dependencies {
    compile project(":database:database-jooq")
    compile project(":internal:shared-model")
    compile libraries.kotlin
    compile libraries.logging
    compile libraries.springCore
    compile libraries.ehcache
    compile libraries.springSecurity
    compile libraries.database

    testCompile libraries.testingLibraries

    integTestCompile libraries.springTest
    integTestCompile libraries.springBootTest
    integTestCompile project(":internal:shared-config")
    integTestCompile project(":internal:version-bumper")
}

integTest.dependsOn ':database:database-migration:flywayMigrate'
