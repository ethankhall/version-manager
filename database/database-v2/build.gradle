apply plugin: 'java'
apply plugin: 'groovy'
apply plugin: 'io.ehdev.gradle.development'
apply plugin: 'kotlin'

dependencies {
    compile project(":database:database-jooq")
    compile project(":internal:shared-model")
    compile deps.libraries.kotlin
    compile deps.libraries.logging
    compile deps.libraries.spring
    compile deps.libraries.ehcache
    compile deps.libraries.database

    testCompile deps.libraries.testingLibraries

    integTestCompile deps.libraries.springTest
    integTestCompile project(":internal:shared-config")
    integTestCompile project(":internal:version-bumper")
}

integTest.dependsOn ':database:database-migration:flywayMigrate'
