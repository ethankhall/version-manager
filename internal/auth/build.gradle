apply plugin: 'java'
apply plugin: 'groovy'
apply plugin: 'io.ehdev.gradle.development'
apply plugin: 'kotlin'

configurations.integTestCompile.exclude module: 'spring-boot-starter-tomcat'

dependencies {
    compile project(":internal:shared-config")
    compile project(":database:database-v2")
    compile deps.libraries.kotlin
    compile deps.libraries.logging
    compile deps.libraries.spring
    compile deps.libraries.common
    compile deps.libraries.commonsLang
    compile deps.libraries.httpClient

    testCompile libraries.testingLibraries

    integTestCompile project(":database:database-migration")
    integTestCompile deps.libraries.database
}
