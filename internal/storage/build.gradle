apply plugin: 'org.jetbrains.kotlin.jvm'
apply plugin: 'groovy'

dependencies {
    compile project(":internal:shared-model")
    compile libraries.kotlin
    compile libraries.googleCloud
    compile libraries.springBoot

    testCompile libraries.springTest
}
