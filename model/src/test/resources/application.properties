# Connect to the DB
spring.datasource.url=jdbc:postgresql://172.0.1.100/version_manager_test
spring.datasource.username=version_manager_test
spring.datasource.password=password
spring.jpa.show-sql=true

# Validate db
spring.jpa.hibernate.ddl-auto=create-drop

#Disable flyway
flyway.enabled=false

