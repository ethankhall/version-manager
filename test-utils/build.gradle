apply plugin: 'groovy'
apply plugin: 'local-dev'

repositories {
    mavenCentral()
}

dependencies {
    compile libraries.groovy
    compile 'org.springframework.restdocs:spring-restdocs-core:1.0.1.RELEASE'
}
