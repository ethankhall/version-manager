# Connect to the DB
spring.datasource.url=jdbc:h2:mem:security;MODE=Postgres;DB_CLOSE_DELAY=-1
spring.jpa.show-sql=true

# Validate db
spring.jpa.hibernate.ddl-auto=create-drop

# Set the management port
management.port=0

#Disable flyway
flyway.enabled=false
