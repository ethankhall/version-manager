
auth:
    client:
        github:
            key: 429f464a7132f07de230
            secret: dccb8a804a0da2a623c4a03b650b36b076a8aea1
        google:
            key: key
            secret: secret

server:
    port: 8080

management:
    context-path: /admin
    port: 8081

jwt:
    signing:
        key: secret

logging:
    level:
        io:
            ehdev: DEBUG
        org:
            pac4j: DEBUG
            jooq: DEBUG

flyway:
    out-of-order: true

spring:
    datasource:
        url: jdbc:postgresql://172.0.1.100/version_manager
        username: version
        password: password
    jpa:
        show-sql: true
        hibernate:
            ddl-auto: verify

---


spring:
    profiles: container
    datasource:
        url: jdbc:postgresql://${DATABASE_URL}/${DATABASE_NAME}
        username: ${DATABASE_USERNAME}
        password: ${DATABASE_PASSWORD}
    jpa:
        show-sql: true
        hibernate:
            ddl-auto: verify
jwt:
    signing:
        key: ${JWT_SIGNING_KEY}

auth:
    callback.url: http://${PUBLIC_ADDRESS}:${PUBLIC_PORT}
    client:
        github:
            key: ${GITHUB_KEY}
            secret: ${GITHUB_SECRET}
        google:
            key: key
            secret: secret
server:
    address: ${PUBLIC_ADDRESS}
    port: ${PUBLIC_PORT}
